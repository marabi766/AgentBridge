<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\AgentBridge.Abstractions\AgentBridge.Abstractions.csproj" />
    <ProjectReference Include="..\AgentBridge.Core\AgentBridge.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Abstractions only, for BackgroundService — TelegramCommandListener needs
         a hosted lifecycle, not the rest of the Hosting package. -->
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.11" />
  </ItemGroup>

</Project>
