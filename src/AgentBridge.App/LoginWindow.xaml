<Window x:Class="AgentBridge.App.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Agent Bridge — Sign in" Icon="Assets/AgentBridgeLogo.png"
        Width="380" Height="280" MinWidth="380" MinHeight="280"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="True">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Agent Bridge" FontSize="20" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" Text="Enter the password to continue." Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,20" />

        <PasswordBox x:Name="PasswordInput" Grid.Row="2" MinHeight="32" Padding="8,5"
                     Background="{DynamicResource PanelBrush}" Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource StrokeBrush}" VerticalContentAlignment="Center" />

        <TextBlock x:Name="ErrorText" Grid.Row="3" Text="Incorrect password." Foreground="{DynamicResource ErrorBrush}" Margin="0,8,0,0" Visibility="Collapsed" />

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Exit" Click="Exit_Click" />
            <Button Content="Sign in" Click="SignIn_Click" Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource AccentTextBrush}" IsDefault="True" Margin="0" />
        </StackPanel>
    </Grid>
</Window>
