<Application x:Class="AgentBridge.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="WindowColor">#F3F3F1</Color>
            <Color x:Key="PanelColor">#FFFFFF</Color>
            <Color x:Key="SubtleColor">#FAF9F7</Color>
            <Color x:Key="StrokeColor">#D8D8D6</Color>
            <Color x:Key="TextColor">#1B1B19</Color>
            <Color x:Key="SecondaryTextColor">#5C5C58</Color>
            <Color x:Key="AccentColor">#005FB8</Color>
            <Color x:Key="WarningColor">#8A5A00</Color>
            <Color x:Key="ErrorColor">#B42318</Color>
            <Color x:Key="VerifiedColor">#157347</Color>

            <SolidColorBrush x:Key="WindowBrush" Color="{StaticResource WindowColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="SubtleBrush" Color="{StaticResource SubtleColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="StrokeBrush" Color="{StaticResource StrokeColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="SecondaryTextBrush" Color="{StaticResource SecondaryTextColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="VerifiedBrush" Color="{StaticResource VerifiedColor}" po:Freeze="False" />
            <SolidColorBrush x:Key="NavigationBrush" Color="#ECECE9" po:Freeze="False" />
            <SolidColorBrush x:Key="ModeBrush" Color="#374151" po:Freeze="False" />
            <SolidColorBrush x:Key="WarningSurfaceBrush" Color="#FFF8E7" po:Freeze="False" />
            <SolidColorBrush x:Key="ErrorSurfaceBrush" Color="#FFF1F0" po:Freeze="False" />
            <SolidColorBrush x:Key="AccentTextBrush" Color="#FFFFFF" po:Freeze="False" />

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="Background" Value="{DynamicResource WindowBrush}" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="Padding" Value="12,4" />
                <Setter Property="Margin" Value="0,0,8,0" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Background" Value="{DynamicResource SubtleBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource SubtleBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
                        <Setter Property="Opacity" Value="0.72" />
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="MinHeight" Value="32" />
                <Setter Property="Padding" Value="8,5" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            </Style>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource StrokeBrush}" />
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource SubtleBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            </Style>
            <Style x:Key="PanelStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Padding" Value="16" />
                <Setter Property="Margin" Value="0,0,12,12" />
            </Style>
            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,0,12" />
            </Style>
            <Style x:Key="Caption" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
